services:
  web:
    build:
      context: .
    ports:
      - "5000:5000"
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - my-network
    environment:
      - FLASK_ENV=production

networks:
  my-network:
      
