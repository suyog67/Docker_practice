services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80"   
    networks:
      - day
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  day:
